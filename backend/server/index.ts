import express from 'express';

// Entry point for the server
const app = express();

// TODO: Add server initialization logic

app.listen(3000, () => {
  console.log('Server is running on port 3000');
});